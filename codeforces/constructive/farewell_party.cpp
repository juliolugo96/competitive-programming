# include <bits/stdc++.h>

using namespace std;

# define io_boost ios::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);

static int a_map[static_cast<int>(1e5)+1];

int main()
{
  io_boost
  int n;
  cin >> n;
  int a[n]{0};
  bool flag;
  for (int i{0}; i < n; ++i)
  {
    cin >> a[i];

  }


  return 0;
}