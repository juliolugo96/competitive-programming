# include <bits/stdc++.h>

using namespace std;

int main()
{
  int m, n;

  cin >> n >> m;
  int dorms[n];
  for (int i{0}; i < n; ++i)
    cin >> dorms[i];
  
  for (int i{0}, j{0}; i < m;++i)
  {
    int rec;
    cin >> rec;

    

  }
  
  return 0;
}
